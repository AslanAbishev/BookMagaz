version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: goodbooks-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: goodbooks

volumes:
  mongo_data:
